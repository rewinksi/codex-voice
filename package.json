{
  "name": "codex-voice",
  "version": "0.1.0",
  "description": "Native Codex voice side-channel plugin with per-thread local STT listeners.",
  "type": "module",
  "private": false,
  "scripts": {
    "test": "node --test",
    "test:unit": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
